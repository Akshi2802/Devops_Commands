Already ubuntu is installed in vagrant box.
Jenkins is installed in ubuntu machine.
Going to install Docker in Ubuntu.
Docker installation: --  Refer:https://docs.docker.com/install/linux/docker-ce/ubuntu/
  Installing Docker Community Edition(free)-CE
   Installing Docker CE using repository -- https://docs.docker.com/install/linux/docker-ce/ubuntu/#install-using-the-repository
   Need to set up the repository and then install/update docker from the repository.
   Set up the repository:
   1.Update the apt package index 
   $ sudo apt-get update
   2.
    
